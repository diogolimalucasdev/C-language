#include <stdio.h>

int main()
{
	int contador =0;
	while (contador < 5)
    {
        printf("seu saldo esta abaixo do preco, seu saldo e %i\n ", contador);
        contador = contador + 1;
    }
    printf("agora seu saldo e suficiente, seu saldo e %i",contador );
  	return 0;
  
}
    
